# Copy this file to `.env.local` (gitignored) and fill in your values.
# Only `VITE_*` variables are exposed to the client bundle.

# Sentry DSN — required in production for error/performance monitoring.
# Free tier signup: https://sentry.io/signup/
# Leave empty in dev to disable Sentry locally.
VITE_SENTRY_DSN=
