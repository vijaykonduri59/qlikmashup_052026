# Normalize line endings to LF for everything except Windows scripts.
* text=auto eol=lf

*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Treat lockfiles as generated to avoid noisy diffs in PRs.
package-lock.json -diff
