// Loaded by Vitest before each test file (configured via `test.setupFiles` in
// vite.config.ts). Importing this submodule extends Vitest's `expect` with
// jest-dom matchers like `toBeInTheDocument`, `toHaveClass`, etc.
import '@testing-library/jest-dom/vitest';
