// Stage 3+: this file is no longer the app entry.
// Routing now lives in src/routes/ and is mounted via RouterProvider in src/main.tsx.
// Safe to delete; left as a stub so the file isn't a surprise in git history.

export {};
